import { buildPageMetadata } from "@/lib/seo/pagesMeta";

export const metadata = buildPageMetadata("independence-day-sale");

export default function IndependenceDaySaleLayout({ children }) {
  return children;
}
