// src/app/payday-sale/layout.jsx
import { buildPageMetadata } from "@/lib/seo/pagesMeta";

export const metadata = buildPageMetadata("payday-sale");

export default function PaydaySaleLayout({ children }) {
  return children;
}
